# openapi-typescript 7.x 宣告 peer typescript@^5，但本專案用 create-vue 帶進的
# TypeScript 6（vue-tsc 3 需要）。openapi-typescript 僅使用穩定的 TS factory API，
# 實測在 TS 6 可正常產生型別，故忽略此過時的 peer 範圍；上游支援 TS 6 後即可移除。
legacy-peer-deps=true
