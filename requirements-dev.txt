# Dev/CI-only tools (ruff, coverage). Not installed into the runtime image;
# the Dockerfile only wheels requirements.txt. Keep this file ASCII-only --
# pip reads it with the system locale (cp950 on zh-Hant Windows); a BOM or
# non-ASCII byte breaks installation.
-r requirements.txt

ruff==0.15.16
coverage==7.14.1
pre-commit==4.6.0
pytest==9.1.1
pytest-django==4.12.0
